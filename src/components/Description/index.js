import React from 'react'
import { useParams } from 'react-router';
const Description = ({item,meal}) => {
    // const {id,meal}=useParams();
    console.log(item,meal,"from desc");
    return (
        <div>
            
        </div>
    )
}

export default Description

import React from 'react'
import { useParams } from 'react-router'
const Profile = ({id}) => {
    const iden=useParams();
    console.log(iden);
    return (
        <div>
            
        </div>
    )
}

export default Profile
